name: Sync fra realfag
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Live er preview-appen
        run: echo "matsamundsen.github.io serverer nå TanStack-previewen. Vanilla-synk er slått av."
